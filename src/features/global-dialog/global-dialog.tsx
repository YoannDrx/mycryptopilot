"use client";

/**
 * DEPRECATED: Global dialog system kept for future use
 * All org-based dialogs removed - Big Bang (Issue #77 Phase 11)
 *
 * This component is used to display global dialogs.
 * Currently no active dialogs - reserved for future features.
 *
 * @returns null (no active dialogs)
 */
export const GlobalDialog = () => {
  // No active dialogs - always return null
  return null;
};
