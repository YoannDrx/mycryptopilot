import type { PropsWithChildren } from "react";

/**
 * Layout pour la section Crypto School
 * Contient: Courses, Lessons, Progress, Certificates
 */
export default function SchoolLayout({ children }: PropsWithChildren) {
  return <>{children}</>;
}
