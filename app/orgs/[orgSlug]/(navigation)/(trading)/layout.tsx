import type { PropsWithChildren } from "react";

/**
 * Layout pour la section Trading
 * Contient: Dashboard, Traders Marketplace, Signals, Pricing, Checkout
 */
export default function TradingLayout({ children }: PropsWithChildren) {
  return <>{children}</>;
}
