import type { PropsWithChildren } from "react";

/**
 * Layout pour la section Tax & Declaration
 * Contient: Import CSV, Tax Reports, Transaction History, Export
 */
export default function TaxLayout({ children }: PropsWithChildren) {
  return <>{children}</>;
}
