# 📊 Audit Documentation & Scripts — 2 novembre 2025

Ce rapport résume l’audit complet exécuté le 2 novembre 2025 (durée ~3h) pour synchroniser la documentation Markdown et les scripts avec l’état réel du code.

---

## ✅ Actions effectuées

### Documentation

- Mise à jour des guides clés : `.claude/CLAUDE.md`, `.claude/docs/DEVELOPMENT.md`, `.claude/docs/DATABASE.md`, `.claude/docs/TRADING-SYSTEM.md`, `.claude/docs/TESTING.md`, `.claude/docs/SUBSCRIPTIONS.md`, `.claude/docs/ENV-VARIABLES-MAPPING.md`, `.claude/docs/CONTEXT7-GUIDE.md`, `README.md`.
- Ajout des précisions `DISCORD_BOT_ENABLED`, `.env.sweep`, TRON API key.
- Clarification des features encore WIP (copy-trade queue, tier-check notifications, validation API exchanges).
- Alignement du périmètre sweep/portfolio/distribution Discord avec le code actuel.

### Scripts

- Transformation de `upgrade-to-pro.ts` / `upgrade-to-ultra.ts` pour utiliser `activateSubscription` avec email + durée en arguments.
- Chargement `.env.local` automatique dans `scripts/start-discord-bot.ts` (fallback `.env` / `.env.development`).
- Documentation `sweep-to-binance.ts` actualisée (transferts opérationnels, dry-run par défaut).
- `scripts/README.md`, `README-SWEEP.md`, `SWEEP_SETUP.md` mis à jour pour refléter le nouvel état.

### Organisation

- Confirmation de la structure actuelle (`scripts/dev-tools/`, `future-features/`, `archive/`).
- Nettoyage des sections obsolètes (ex : TODO sweep, doc tests héritée).

---

## 📚 Inventaire Markdown (39 fichiers)

| Catégorie                               | Statut | Notes                                                         |
| --------------------------------------- | ------ | ------------------------------------------------------------- |
| `.claude/CLAUDE.md`                     | ✅     | Date & TODOs réalignés, priorités mises à jour.               |
| `.claude/docs/DEVELOPMENT.md`           | ✅     | Nouvelle synthèse, roadmap concise.                           |
| `.claude/docs/DATABASE.md`              | ✅     | Regroupement par domaines & commandes utiles.                 |
| `.claude/docs/TRADING-SYSTEM.md`        | ✅     | Vue macro du trading unifié et copy-trade.                    |
| `.claude/docs/TESTING.md`               | ✅     | Playbook tests (unitaires + E2E) rafraîchi.                   |
| `.claude/docs/SUBSCRIPTIONS.md`         | ✅     | Mention referral bonus + scripts admin refactor.              |
| `.claude/docs/ENV-VARIABLES-MAPPING.md` | ✅     | Ajout `.env.sweep`, `TRON_API_KEY`, `DISCORD_BOT_ENABLED`.    |
| `.claude/docs/CONTEXT7-GUIDE.md`        | ✅     | Bibliothèques supplémentaires (ccxt, @scure, @ai-sdk).        |
| `.claude/docs/DEPLOYMENT.md`            | ✅     | Variables Railway enrichies.                                  |
| `README.md`                             | ✅     | Présentation publique mise à jour, liens docs valides.        |
| `.claude/docs/future-features/*`        | 📂     | Specs non implémentées (referral, user lifecycle) conservées. |
| `.claude/docs/archive/*`                | 📦     | Historiques (navigation, archi) conservés pour référence.     |

> Aucun fichier Markdown résiduel obsolète détecté après l’audit.

---

## 🧰 Inventaire Scripts (23 fichiers)

| Groupe             | Scripts                                                                | Statut                                                 |
| ------------------ | ---------------------------------------------------------------------- | ------------------------------------------------------ |
| Crypto & paiements | `generate-*-xpubs.ts`, `sweep-to-binance.ts`, `verify-encryption.ts`   | ✅ À jour (sweep dry-run/production).                  |
| Testing & setup    | `setup-test-db.sh`, `run-e2e-tests.sh`                                 | ✅ Utilisés par CI/local.                              |
| Admin plans        | `upgrade-to-pro.ts`, `upgrade-to-ultra.ts`, `clean-resend-audience.ts` | ✅ Refactor args/activateSubscription.                 |
| Portfolio / DB     | `backfill-trader-trade-quantities.ts`                                  | ⚠️ One-shot — archiver après exécution globale.        |
| Discord            | `start-discord-bot.ts`, `deploy-railway.sh`                            | ✅ Aligned; doc mise à jour.                           |
| Conductor          | `conductor-setup-script.sh`, `conductor-archive-script.sh`             | ❓ À confirmer (conserver ou archiver).                |
| Dev-tools          | `scripts/dev-tools/*`                                                  | ✅ Outils debug (adresse, checkout, RPC, DB, cleanup). |

Points de vigilance restants :

1. Décider du retrait ou non des scripts Conductor (plus utilisés depuis adoption Git worktrees ?).
2. Déplacer `backfill-trader-trade-quantities.ts` vers `scripts/archive/` dès que la migration est validée sur tous les environnements.
3. Ajouter éventuellement un script de monitoring sweep (export CSV ou dashboard) pour faciliter la comptabilité.

---

## 🔜 Prochaines améliorations suggérées

1. **Copy-trade automation** : implémenter la queue d’exécution (TODO dans `copy-trade.service.ts`).
2. **Validation API utilisateur** : remplacer les stubs par de vraies requêtes exchange (TODO `user-exchange-connection.service.ts`).
3. **Notifications tier-check / email hebdo** : compléter `tier-check-job.ts` et `email-notifications.ts`.
4. **Env sync** : étendre `.claude/commands/sync-env.md` pour inclure `.env.sweep` (lecture seule) et `TRON_API_KEY`.

---

## 📎 Annexes

- Rapport détaillé de mise à jour : git diff courant (`git status` / `git diff`).
- Documentation associée : voir tableau ci-dessus.

_Rapport tenu à jour en même temps que les modifications — ne pas hésiter à modifier cette page lors du prochain audit._
